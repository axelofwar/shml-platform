# PostgreSQL with pgvector extension for RAG memory system
# Use the official pgvector image based on postgres 15
FROM pgvector/pgvector:pg15

# The pgvector extension is already installed
# Just need to ensure it's available
