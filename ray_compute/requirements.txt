# Ray Compute API - Python Dependencies
# Install with: pip install -r requirements.txt
# Updated: 2024-11-30 - Ray 2.35.0 + MLflow 3.6.0 + pydantic v2

# Ray 2.35.0 (supports pydantic v2, compatible with MLflow 3.x)
ray[default]==2.35.0

# Core API Framework (pydantic v2 compatible)
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.5.0,<3
python-multipart==0.0.12

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.10
alembic==1.12.1

# OAuth & Auth
python-jose[cryptography]==3.3.0
httpx==0.27.0

# Redis (Job Queue)
redis>=5.0.1

# Monitoring
prometheus-client>=0.19.0

# Notifications
apprise==1.6.0

# ML/Data - MLflow 3.6.0 (now compatible with Ray 2.35+ and pydantic v2)
mlflow>=3.6.0
pandas==2.1.3
numpy==1.26.2

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
