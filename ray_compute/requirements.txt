# Ray Compute API - Python Dependencies
# Install with: pip install -r requirements.txt
# Updated: 2024-12-05 - Ray 2.35.0 + MLflow 3.6.0 + Ultralytics YOLO
#
# CRITICAL: numpy<2.0 required for opencv-python-headless binary compatibility
# Package order matters - install numpy first!

# Ray 2.35.0 (supports pydantic v2, compatible with MLflow 3.x)
ray[default]==2.35.0

# Core API Framework (pydantic v2 compatible)
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.5.0,<3.0.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.10
asyncpg>=0.29.0
alembic==1.12.1

# GraphQL
strawberry-graphql[fastapi]>=0.234.0

# OAuth & Auth
python-jose[cryptography]==3.3.0
httpx==0.27.0

# Redis (Job Queue)
redis>=5.0.1

# Monitoring
prometheus-client>=0.19.0

# Notifications
apprise==1.6.0

# ==============================================================================
# ML/Data - CRITICAL: numpy<2.0 for binary compatibility with opencv
# ==============================================================================
numpy>=1.24.0,<2.0.0
pandas==2.0.3
pyarrow==14.0.1

# MLflow 3.x (compatible with Ray 2.35+ and pydantic v2)
mlflow>=3.6.0,<4.0.0

# Ultralytics YOLO - Object Detection
ultralytics==8.0.200

# OpenCV Headless (no GUI dependencies, compiled against numpy 1.x)
opencv-python-headless==4.8.1.78

# ==============================================================================
# Utilities
# ==============================================================================
python-dotenv==1.0.0
pyyaml==6.0.1
requests==2.31.0
tqdm==4.66.1

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
