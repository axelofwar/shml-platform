services:
  nemotron-coding:
    image: ${SHML_REGISTRY_IMAGE_PREFIX:-shml/local}/nemotron-coding:${SHML_IMAGE_TAG:-latest}
    pull_policy: ${SHML_IMAGE_PULL_POLICY:-missing}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nemotron-coding
    restart: unless-stopped

    environment:
      - NVIDIA_VISIBLE_DEVICES=0 # RTX 3090 Ti only
      - CUDA_VISIBLE_DEVICES=0

    volumes:
      - ../../data/models/nemotron-3:/models:ro

    command:
      - --model
      - /models/Nemotron-3-Nano-30B-A3B-UD-Q4_K_XL.gguf
      - --alias
      - nemotron-coding
      - --threads
      - "-1"
      - --n-gpu-layers
      - "999"
      - --ctx-size
      - "65536"
      # KV cache quantization (TurboQuant-aligned: arXiv:2504.19874 validates 3.5-bit quality-neutral)
      # Architecture (verified from GGUF metadata): nemotron_h_moe, 52 total layers.
      #   Only 6 are hybrid attention layers (at positions 5,12,19,26,33,42); 46 are Mamba2 SSM.
      #   KV heads per attn layer = 2 (aggressive GQA), head_dim = 128.
      #
      # VRAM budget at ctx=65536, 22.8 GB weights → 1,217 MiB headroom on RTX 3090 Ti:
      #   KV f16 (default): 384 MiB → 633 MiB remaining  (safe, but tight)
      #   KV q8_0 (applied): 192 MiB → 825 MiB remaining  (+192 MiB headroom vs f16)
      #   KV q4_0 (optional): 96 MiB → 921 MiB remaining
      # q8_0 is the sweet spot: saves 192 MiB of VRAM, still quality-neutral per TurboQuant.
      - --cache-type-k
      - q8_0
      - --cache-type-v
      - q8_0
      # To reduce further (still safe): change both to q4_0 (saves 288 MiB vs f16)
      - --port
      - "8000"
      - --host
      - 0.0.0.0
      - --jinja
      - --temp
      - "0.6"
      - --top-p
      - "0.95"
      - --reasoning-budget
      - "0"

    # SECURITY: No direct port exposure - access through Traefik only
    # ports:
    #   - "8010:8000"  # REMOVED: was bypassing auth

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: [ '0' ]
              capabilities: [ gpu ]

    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nemotron.rule=PathPrefix(`/api/coding`)"
      - "traefik.http.routers.nemotron.priority=2147483647"
      - "traefik.http.services.nemotron.loadbalancer.server.port=8000"
      - "traefik.http.middlewares.nemotron-strip.stripprefix.prefixes=/api/coding"
      - "traefik.http.routers.nemotron.middlewares=oauth2-errors,oauth2-auth,nemotron-strip"

    networks:
      - shml-platform

    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # ==========================================================================
  # Nemotron Manager - GPU Yield API for Training Integration
  # Provides /training/start and /training/end endpoints for Ray jobs
  # ==========================================================================
  nemotron-manager:
    build:
      context: ./manager
      dockerfile: Dockerfile
    container_name: nemotron-manager
    restart: unless-stopped

    environment:
      - NEMOTRON_CONTAINER=nemotron-coding
      - GPU_INDEX=0
      - YIELD_WAIT_SECONDS=5
      - DOCKER_HOST=tcp://docker-proxy:2375

    # SECURITY: Docker socket removed - using proxy instead
    # volumes:
    #   - /var/run/docker.sock:/var/run/docker.sock  # REMOVED: host-escape vector

    # Run with docker group for socket access
    user: "1000:984" # UID:docker-GID (adjust GID if different)

    # SECURITY: No direct port exposure - access through Traefik only
    # ports:
    #   - "8011:8000"  # REMOVED: was bypassing auth

    networks:
      - shml-platform

    labels:
      - "traefik.enable=true"
      # Training yield API - accessible to Ray jobs
      - "traefik.http.routers.nemotron-manager.rule=PathPrefix(`/api/coding-manager`)"
      - "traefik.http.routers.nemotron-manager.priority=2147483646"
      - "traefik.http.services.nemotron-manager.loadbalancer.server.port=8000"
      - "traefik.http.middlewares.nemotron-manager-strip.stripprefix.prefixes=/api/coding-manager"
      - "traefik.http.routers.nemotron-manager.middlewares=oauth2-errors,oauth2-auth,nemotron-manager-strip"

    healthcheck:
      test: [ "CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    depends_on:
      - nemotron-coding

networks:
  shml-platform:
    external: true
