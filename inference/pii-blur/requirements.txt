# FastAPI framework - pinned for fast resolution
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Core dependencies - numpy installed separately in Dockerfile first
scipy==1.11.4

# Computer vision - pinned
opencv-python-headless==4.9.0.80
Pillow==10.2.0

# PyTorch ecosystem - all pinned to same CUDA version
torch==2.1.0
torchvision==0.16.0

# YOLO - pin specific version to avoid iteration
# YOLOv8m-Face model downloaded from HuggingFace at runtime
ultralytics==8.1.0

# HuggingFace for model downloads
huggingface_hub==0.20.3

# ByteTrack for temporal consistency - use lapx (maintained fork with wheels)
lapx==0.5.10.post1

# Video processing - pinned
moviepy==1.0.3
imageio==2.33.1
imageio-ffmpeg==0.4.9

# Utilities - pinned
pydantic==2.5.3
pydantic-settings==2.1.0
aiofiles==23.2.1

# Database
asyncpg==0.29.0
redis==5.0.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
