[Unit]
Description=GitNexus Reindex Timer (on commit, every 4h fallback)

[Timer]
# Fallback: reindex every 4 hours in case hooks missed changes
OnCalendar=*-*-* 00/4:00:00
Persistent=true

[Install]
WantedBy=timers.target
